{
  "name": "@gazette/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev:server & bun run dev:worker",
    "dev:server": "bun --env-file=.env run --hot src/index.ts",
    "dev:worker": "bun --env-file=.env run --hot src/queue/worker.ts",
    "build": "bun build src/index.ts src/queue/worker.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js",
    "worker": "bun run src/queue/worker.ts",
    "test": "bun test",
    "lint": "prettier --check \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf node_modules dist data",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@gazette/shared": "workspace:*",
    "@google/genai": "^1.17.1",
    "@hono/zod-validator": "^0.7.6",
    "bullmq": "^5.25.0",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.1",
    "ioredis": "^5.4.1",
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1",
    "puppeteer": "^24.34.0",
    "sharp": "^0.33.5",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.3.3"
  }
}
